# Server Configuration
PORT=8765
AUTH_ENABLED=true

# Database path (optional, defaults to data/training.db)
# DB_PATH=/custom/path/to/training.db

# Google OAuth (Required if AUTH_ENABLED=true)
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Default: http://localhost:8765/auth/google/callback
# GOOGLE_REDIRECT_URI=https://your-domain.com/auth/google/callback

# OpenAI API (Optional, for "AI Coach" feature)
# OPENAI_API_KEY=sk-your-openai-api-key

# Migration Configuration (Only needed for migrate.js)
# NOTION_API_KEY=secret_...
# DATABASE_ID=...
